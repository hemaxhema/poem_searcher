# CLAUDE.md

- Do not run the app (e.g. `flutter run`, launching it directly) in this project. Use static checks (analyze, build, tests) to verify changes instead. If actually running the app is needed to verify something, ask first.

- if you want to run terminal code like dartvm, give me the code i will run it.